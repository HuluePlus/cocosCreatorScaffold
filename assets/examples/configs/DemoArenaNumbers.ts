/**
 * 示例场地与设计分辨率数值。
 * 这里只声明不可变数据，不放置场地计算、布局流程或运行时逻辑。
 */
export const DEMO_ARENA_NUMBERS = {
  designWidth: 375,
  designHeight: 852,
  interfaceScale: 0.5,
  width: 650,
  height: 650,
} as const;
